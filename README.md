# tanishsidhu.github.io (legacy redirect)

This repository previously hosted the portfolio. It now only redirects to:

**https://tanishsidhu.pages.dev**

The full source lives in the private repository [tanishsidhu/portfolio](https://github.com/tanishsidhu/portfolio).

`index.html` and `404.html` redirect visitors (including path, query, and hash) to the new site so existing shared links keep working. `robots.txt` disallows indexing of these redirect pages.
